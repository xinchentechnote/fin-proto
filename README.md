# fin-proto
# DSL语法说明

## 概述

这是一个用于定义二进制数据包结构的领域特定语言(DSL)，主要用于描述金融交易系统中的消息格式。该DSL基于ANTLR4语法定义，支持定义数据结构、元数据类型和匹配规则。

## 基本结构

### 1. 包定义(Packet Definition)

```
root? packet PacketName {
    fieldDefinitions
}
```

- `root`(可选): 标识这是根数据包
- `packet`: 关键字，开始一个包定义
- `PacketName`: 包名称(标识符)
- `fieldDefinitions`: 包含字段定义

### 2. 元数据定义(Metadata Definition)

```
MetaData DataTypeName {
    type? fieldName `描述`,?
}
```

- `MetaData`: 关键字，开始元数据类型定义
- `DataTypeName`: 元数据类型名称
- `type`: 字段类型(可选，默认为标识符)
- `fieldName`: 字段名称
- `` `描述` ``: 字段描述(可选)
- `,`: 可选的分隔符

## 字段类型

### 基本类型

| 类型      | 别名  | 描述       |
| ------- | --- | -------- |
| uint8   | u8  | 8位无符号整数  |
| uint16  | u16 | 16位无符号整数 |
| uint32  | u32 | 32位无符号整数 |
| uint64  | u64 | 64位无符号整数 |
| int8    | i8  | 8位有符号整数  |
| int16   | i16 | 16位有符号整数 |
| int32   | i32 | 32位有符号整数 |
| int64   | i64 | 64位有符号整数 |
| float32 | f32 | 32位浮点数   |
| float64 | f64 | 64位浮点数   |
| char    |     | 字符       |
| string  |     | 变长字符串      |
| char[]  |     | 变长字符串      |
| char[n] |     | 固定长度字符数组 |

### 复合类型

1. ​**​内联对象定义​**​:  
   **CODEBLOCK2**

2. ​**​重复字段​**​:  
   **CODEBLOCK3**  
   或  
   **CODEBLOCK4**

3. ​**​匹配字段​**​:  
   **CODEBLOCK5**
   
   - 支持单个值和列表值匹配
   - 值可以是数字或字符串

## 示例

### 基本包定义

**CODEBLOCK6**

### 元数据类型定义

**CODEBLOCK7**

### 复杂结构

**CODEBLOCK8**

## 编写指南

1. ​**​命名规范​**​:
   
   - 使用驼峰命名法或下划线分隔
   - 保持命名一致性

2. ​**​注释​**​:
   
   - 使用`//`进行单行注释
   - 字段描述使用反引号`` `描述` ``

3. ​**​最佳实践​**​:
   
   - 为常用数据类型定义元数据类型
   - 使用匹配字段处理消息类型分发
   - 对重复结构使用内联对象定义
   - 为重要字段添加描述

4. ​**​验证​**​:
   
   - 确保所有引用类型已定义
   - 检查字段类型是否匹配实际数据
   - 验证匹配字段是否覆盖所有可能值

## 注意事项

- 大小写敏感(除类型别名外)
- 字段分隔符`,`可选
- 字符串使用双引号`"`，描述使用反引号`` ` ``
- 空白字符(空格、制表符、换行)会被忽略

这个DSL语法提供了灵活的方式来定义复杂的二进制协议结构，特别适合金融交易系统中的消息格式描述。
